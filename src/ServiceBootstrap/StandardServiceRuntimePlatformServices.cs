using System.Reflection;

namespace ServiceBootstrap
{
    static class StandardServiceRuntimePlatformServices
    {
        public static void Register(Assembly assembly = null)
        {
            // anything related to the platform we executed on should go here
        }
    }
}